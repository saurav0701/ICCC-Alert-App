<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Storage permissions for screenshots -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- OSM Map Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- PiP Support -->
    <uses-feature
        android:name="android.software.picture_in_picture"
        android:required="false" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ICCCAlertApp"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- No Google Maps API Key needed - Using OpenStreetMap with Google tiles -->

        <!-- FileProvider for sharing files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- WebSocket Service -->
        <service
            android:name=".WebSocketService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false"
            android:directBootAware="true" />

        <!-- Boot Receiver -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- Doze Mode Exemption Receiver -->
        <receiver
            android:name=".DozeExemptionReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.os.action.DEVICE_IDLE_MODE_CHANGED" />
                <action android:name="android.os.action.POWER_SAVE_MODE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ICCCAlertApp"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Registration Activity -->
        <activity
            android:name=".RegistrationActivity"
            android:label="Register"
            android:parentActivityName=".LoginActivity"
            android:theme="@style/Theme.ICCCAlertApp"
            android:exported="false"
            android:noHistory="true" />

        <!-- OTP Verification Activity -->
        <activity
            android:name=".OTPVerificationActivity"
            android:label="Verify OTP"
            android:parentActivityName=".LoginActivity"
            android:theme="@style/Theme.ICCCAlertApp"
            android:exported="false" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ICCCAlertApp" />

        <!-- Profile Activity -->
        <activity
            android:name=".ProfileActivity"
            android:label="@string/profile_title"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.ICCCAlertApp"
            android:exported="false" />

        <!-- Map Activity - OSM -->
        <activity
            android:name=".MapActivity"
            android:label="Event Location"
            android:parentActivityName=".ChannelDetailActivity"
            android:theme="@style/Theme.ICCCAlertApp"
            android:exported="false" />

        <!-- Camera Map Activity - Google Maps -->
        <activity
            android:name=".CameraMapActivity"
            android:label="Camera Map"
            android:parentActivityName=".CameraStreamsActivity"
            android:theme="@style/Theme.ICCCAlertApp"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- ✅ FIXED: Multi-Camera Views - Using MaterialComponents theme -->
        <activity
            android:name=".MultiCameraViewsActivity"
            android:parentActivityName=".CameraStreamsActivity"
            android:label="Multi-Camera Views"
            android:theme="@style/Theme.MaterialComponents.Light.DarkActionBar"
            android:exported="false" />

        <!-- ✅ FIXED: Create Multi-Camera View - Using MaterialComponents theme -->
        <activity
            android:name=".CreateMultiCameraViewActivity"
            android:parentActivityName=".MultiCameraViewsActivity"
            android:label="Create Quad View"
            android:theme="@style/Theme.MaterialComponents.Light.DarkActionBar"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />

        <!-- Quad Camera Player (2x2 Grid) -->
        <activity
            android:name=".QuadCameraPlayerActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:parentActivityName=".MultiCameraViewsActivity"
            android:screenOrientation="unspecified"
            android:label="Quad View"
            android:exported="false" />

        <!-- Image Viewer Activity -->
        <activity
            android:name=".ImageViewerActivity"
            android:label="Event Image"
            android:parentActivityName=".ChannelDetailActivity"
            android:theme="@style/Theme.ICCCAlertApp"
            android:exported="false"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize" />

        <!-- Channel Detail Activity -->
        <activity
            android:name=".ChannelDetailActivity"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:theme="@style/Theme.ICCCAlertApp"
            android:exported="false" />

        <!-- Search Activity -->
        <activity
            android:name=".SearchActivity"
            android:label="Discover Channels"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.ICCCAlertApp"
            android:exported="false" />

        <!-- Subscriptions Activity -->
        <activity
            android:name=".SubscriptionsActivity"
            android:label="Your Channels"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.ICCCAlertApp"
            android:exported="false" />

        <!-- Alerts Activity -->
        <activity
            android:name=".AlertsActivity"
            android:label="Live Alerts"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.ICCCAlertApp"
            android:exported="false" />

        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:label="Settings"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.ICCCAlertApp"
            android:exported="false" />

        <!-- Saved Messages Activity -->
        <activity
            android:name=".SavedMessagesActivity"
            android:label="Saved Messages"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.ICCCAlertApp"
            android:exported="false" />

        <!-- Camera Streams Activity -->
        <activity
            android:name=".CameraStreamsActivity"
            android:label="Camera Streams"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.ICCCAlertApp"
            android:exported="false" />

        <!-- Camera Grid View Activity -->
        <activity
            android:name=".CameraGridViewActivity"
            android:label="Camera Grid View"
            android:parentActivityName=".CameraStreamsActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.ICCCAlertApp"
            android:exported="false" />

        <activity
            android:name=".CameraStreamPlayerActivity"
            android:label="Camera Stream"
            android:parentActivityName=".CameraStreamsActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.ICCCAlertApp"
            android:hardwareAccelerated="true"
            android:exported="false"
            android:launchMode="singleTask"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:stateNotNeeded="true"
            android:persistableMode="persistNever">

            <!-- PiP window configuration -->
            <layout
                android:defaultHeight="500dp"
                android:defaultWidth="500dp"
                android:gravity="top|end"
                android:minHeight="100dp"
                android:minWidth="100dp" />

            <!-- PiP metadata -->
            <meta-data
                android:name="android.webkit.WebView.EnableSafeBrowsing"
                android:value="false" />
        </activity>

    </application>

</manifest>